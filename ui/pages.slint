// By: Eric MacDonald (eMac)

import { 
	StartPage, UIMAMEOptions
} from "./start.slint";
import { 
	SetupPage, UIPaths
} from "./setup.slint";
import { 
	AboutPage, UIAbout
} from "./about.slint";

export { 
	StartPage, UIMAMEOptions,
	SetupPage, UIPaths,
	AboutPage, UIAbout
}
